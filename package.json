{
  "name": "current-game",
  "version": "0.2.0",
  "private": true,
  "description": "Current - draw copper traces, real resistance and heat decide whether the lamp lights",
  "scripts": {
    "build": "node tools/build.js",
    "test": "npm run build && node tests/physics.test.js && node tests/e2e.test.js",
    "test:physics": "npm run build && node tests/physics.test.js",
    "test:e2e": "npm run build && node tests/e2e.test.js",
    "shots": "npm run build && node tools/shots.js",
    "sweep": "npm run build && node tools/sweep.js",
    "package": "node tools/package-portal.js"
  },
  "devDependencies": { "playwright": "^1.49.0" }
}
